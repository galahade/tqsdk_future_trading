version: "3.8"
services:
  app_test:
    image: galahade/future-trade-docker
    command: python3 main.py -t -s 2018 -tt 0
    working_dir: /app
    environment:
      MONGO_HOST: mongo
      MONGO_ADMINUSERNAME: root
      MONGO_ADMINPASSWORD: example
      MONGO_PORT: 27017

networks:
  default:
    name: tqsdk_future_trading_future_trading
    external: true
